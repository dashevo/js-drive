describe('fetchDapObjectsMethodFactory', () => {
  it('should return DAP objects');
  it('should return emty array in case no DAP objects where found');
  it('should return an error in case DAP ID is invalid');
  it('should return an error in case filtering options are invalid');
});
