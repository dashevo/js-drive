describe('attachBlockErrorsHandler', () => {
  it('should throw error if skipBlockWithErrors is disabled');
  it('should restart reader from the next block if skipBlockWithErrors option is set');
  it('should not restart reader if the current block is the last one');
});
