describe('addSTPacketMethodFactory', () => {
  it('should add ST packet and return its CID');
  it('should return an error if parameter is missing');
  it('should return an error if parameter is invalid');
});
